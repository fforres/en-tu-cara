allowBuilds:
  # core-js's only build script is a postinstall donation/ad banner — nothing we
  # need. posthog-js pulls it in transitively; we acknowledge it as not-built.
  core-js: false
  esbuild: true
  lefthook: true

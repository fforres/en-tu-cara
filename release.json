{
  "_comment": "Single source of truth for the app version. Bump with `pnpm release <patch|minor|major>` (it propagates to package.json, src-tauri/tauri.conf.json, src-tauri/Cargo.toml). Merging a version CHANGE here to main is what cuts a GitHub release — ordinary merges do nothing. See docs/RELEASING.md.",
  "version": "0.6.0",
  "notes": "En Tu Cara 0.6.0 — send suggestions right from the app (macOS, Apple Silicon).\n\nFeedback:\n- A new Comments button (💬) in the menu-bar popover opens a suggestion box, so you can send ideas, bug reports, or anything else without leaving En Tu Cara.\n- Adding your email is optional — only if you'd like a reply — and feedback sends even when usage stats are turned off, since it's something you chose to send.\n- Suggestions are delivered in the background and never block your alerts."
}

{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "updater",
  "description": "Self-update: check GitHub releases, download + install, relaunch.",
  "windows": ["*"],
  "permissions": ["updater:default", "process:default", "notification:default", "store:default"]
}
